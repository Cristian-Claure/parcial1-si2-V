import { describe, expect, it, vi } from "vitest"; import type { AccessContextService } from "../common/authz/access-context.service.js"; import type { CheckoutRepository } from "./checkout.repository.js"; import { CheckoutService } from "./checkout.service.js";
const p={userId:"30000000-0000-4000-8000-000000000001",email:"c@test",role:"CUSTOMER" as const,name:"C"}; const c="10000000-0000-0000-0000-000000000001"; const a={resolve:vi.fn().mockResolvedValue({userId:p.userId,role:"CUSTOMER",storeId:null,companyId:null})} as unknown as AccessContextService;
describe("Checkout modality eligibility",()=>{it("separates pickup and delivery",async()=>{const r={activeCartLines:vi.fn().mockResolvedValue([{variantId:"v",quantity:2}]),activeDefaultWarehouses:vi.fn().mockResolvedValue([{warehouseId:"w1",warehouseCode:"M",warehouseName:"Main",storeId:"s1",storeName:"S",storeAddress:null}]),canFulfillPickup:vi.fn().mockResolvedValue(false),canFulfillDelivery:vi.fn().mockResolvedValue(true)} as unknown as CheckoutRepository; const out=await new CheckoutService(r,a).eligibleWarehouses(p,c); expect(out[0]).toMatchObject({pickupEligible:false,deliveryEligible:true});});});
